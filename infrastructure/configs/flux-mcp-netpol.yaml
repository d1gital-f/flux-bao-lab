apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: flux-mcp-access
  namespace: flux-system
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: flux-operator-mcp
      app.kubernetes.io/instance: flux-operator-mcp
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector: {}
        - ipBlock:
            cidr: 192.168.252.0/24
        - ipBlock:
            cidr: 10.42.0.0/16
      ports:
        - port: 9090
          protocol: TCP
